# Royal Mail Click & Drop API key
# Get yours from: Click & Drop → Settings → API credentials
RM_API_KEY=your-royal-mail-api-key

# Live endpoint (default — no sandbox available)
RM_BASE_URL=https://api.parcel.royalmail.com/api/v1
